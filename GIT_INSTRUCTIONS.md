# 🆙 GitHub Push Instructions

Since the automated environment did not have `git` pre-configured, please follow these simple steps to push your high-end portfolio to GitHub:

1. **Open PowerShell/Terminal** in the project folder:
   `g:\내 드라이브\99.Develop\한화오션\hanwha-ocean-asset-monitor`

2. **Initialize & Commit**:
   ```powershell
   git init
   git add .
   git commit -m "feat: Initial Enterprise AX Monitoring System v1.0"
   ```

3. **Add Remote & Push**:
   ```powershell
   git remote add origin https://github.com/glory903-devsecops/hanwha-ocean-asset-monitor.git
   git branch -M main
   git push -u origin main
   ```

*Note: You may need to log in to your GitHub account during the push process.* 
🚀 **Your premium portfolio is now ready for the Hanwha Ocean AX team!**
